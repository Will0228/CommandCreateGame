name: Simple Workflow

on: 
  push :

  workflow_dispatch: 
    inputs:
      CONFIGUREATION:
        description: '構成の選択'
        required: true
        default: Debug
        type: choice
        options:
          - Debug
          - Release

jobs:
  Example:
      runs-on: macos-latest

      steps:
        - run: echo "${{ inputs.CONFIGURATION }}"
